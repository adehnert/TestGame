\Preamble{html}

\ConfigureList{itemz}
{\ifvmode\IgnorePar\fi\EndP\HCode{<ul class="itemz">}\ShowPar\bgroup\def\finli{}}
{\ifvmode\IgnorePar\fi\EndP\finli\egroup\HCode{</ul>}}
{\ShowPar\finli\HCode{<li>}\par\def\finli{\EndP\HCode{</li>}}\DeleteMark}{}

\ConfigureList{members}
{\ifvmode\IgnorePar\fi\EndP\HCode{<ul class="itemz members">}\ShowPar\bgroup\def\finli{}}
{\ifvmode\IgnorePar\fi\EndP\finli\egroup\HCode{</ul>}}
{\ShowPar\finli\HCode{<li>}\par\def\finli{\EndP\HCode{</li>}}\DeleteMark}{}

\ConfigureList{contacts}
{\ifvmode\IgnorePar\fi\EndP\HCode{<ul class="itemz contacts">}\ShowPar\bgroup\def\finli{}}
{\ifvmode\IgnorePar\fi\EndP\finli\egroup\HCode{</ul>}}
{\ShowPar\finli\HCode{<li>}\par\def\finli{\EndP\HCode{</li>}}\DeleteMark}{}

\ConfigureList{tlist}
{\ifvmode\IgnorePar\fi\EndP\HCode{<ul class="itemz tlist">}\ShowPar\bgroup\def\finli{}}
{\ifvmode\IgnorePar\fi\EndP\finli\egroup\HCode{</ul>}}
{\ShowPar\finli\HCode{<li>}\par\def\finli{\EndP\HCode{</li>}}\DeleteMark}{}

\makeatletter
% For some reason, this chokes (I guess it produces output, and then complains
% that \begin{document} hasn't happened yet?
%\Configure{title@display}{\HCode{<h2 class='title'>}}{\HCode{</h2>}}

% However, this does work (ish), in combination with some hacks in the
% HTML->epub generation
\long\def\htlatex@title@display#1{{%
    {\HCode{<h2 class='chapter'>}{#1}\HCode{</h2>}}%
  }}

  \def\title@display{\htlatex@title@display}
\makeatother

% For some reason, the lists in here don't work. They're not really essential,
% so for now just clobber them.
\def\charsheetlists{}


\begin{document}

\EndPreamble
